#! /usr/bin/bash

sleep 2700 && notify-send "break"
sleep 300 && notify-send "back to work"
sleep 2700 && notify-send "break"
sleep 300 && notify-send "back to work"
sleep 2700 && notify-send "break"
sleep 300 && notify-send "back to work"
sleep 2700 && notify-send "break"
sleep 900 && notify-send "back to work"
